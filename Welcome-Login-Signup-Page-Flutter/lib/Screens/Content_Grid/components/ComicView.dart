import 'package:flutter/material.dart';

class ComicView extends StatelessWidget {
  const ComicView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build of the view of the comic in the grid

    throw UnimplementedError();
  }
}